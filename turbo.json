{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^db:generate", "^^build", "^build"],
      "env": ["PORT", "HOST", "DB_URL"],
      "outputs": ["dist/**", ".next/**", "out/**"]
    },
    "dev": {
      "dependsOn": ["^db:generate"],
      "cache": false
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "clean": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "db:generate": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "db:push": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "db:push:fdl": {
      "dependsOn": ["^build"],
      "cache": false
    }
  },
  "globalEnv": ["NODE_ENV"]
}
